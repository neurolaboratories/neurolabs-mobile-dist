This repository serves as a binary distribution point for Swift Package Manager (SPM).
It provides prebuilt binary frameworks via GitHub Releases, allowing zia-capture to integrate them easily without compiling from source.
